{
  "$schema": "https://schema.nixpacks.com/nixpacks.schema.json",
  "providers": ["node"],
  "buildCmd": "pnpm install --frozen-lockfile && pnpm --filter @mysociety/resident build",
  "startCmd": "pnpm --filter @mysociety/resident start"
}
